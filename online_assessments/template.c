// Problem Taken from:
//
#include <assert.h>
#include <limits.h>
#include <math.h>
#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    /* 1D Array */
    // int* arr = (int *)malloc(sizeof(int)*arr_size);

    /* 2D Array */
    // int **arr = (int **)malloc(arr_row_size * sizeof(int *));
    // for (int i = 0; i < arr_row_size; i++) {
    //     arr[i] = (int *)malloc(arr_col_size * sizeof(int));
    // }

    // for (int i = 0; i < arr_row_size; i++) {
    //     int *curr = arr[i];
    //     free(curr);
    // }
    // free(arr);

    // printf("%d\n",a); // print int
    // printf("%ld\n",); // print long
    // printf("%f\n",); // print float
    // printf("%e\n",); // print double
    // printf("%c\n",); // print char
    // printf("%s\n",); // print string
}